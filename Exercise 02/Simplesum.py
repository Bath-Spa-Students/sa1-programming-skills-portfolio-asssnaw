# We declare the first variable NO1 with the value '8'
NO1 = 8

# We declare the second variable NO2 with the value '10'
NO2 = 10

# Now we add both the variable interger values
Sum = (NO1 + NO2)

# In the final step we print the sum to get the output
print(Sum)
